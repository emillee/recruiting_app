<%= render 'sidebar' %>

<div class="profile-wrapper">
	<div class="userindex-snapshot-wrapper">
		<% @users.each do |user| %>
			<li>
				<%= render './cards/users/bcard', locals: { user: user }%>
			</li>
		<% end %>
	</div>
</div>

<div class="darkfabric-background-overlay"></div>