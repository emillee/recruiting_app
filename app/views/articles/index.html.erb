<%= render "./layouts/inpage_navbar" %>

<ul class="links">
	<% @articles.each do |article| %>
		<li>
			<%= render 'article_preview', locals: { article: article, this_table: 'users' } %>
		</li>
	<% end %>
</ul>
