<li>
	<a href="<%= job.link %>">
		<img src="<%= asset_path( 'betaworks.jpeg' ) %>" />
		<div class="description">
			<p><%= job.title %> | <%= job.company %></p>
			<p><%= job.description %></p>
		</div>
	</a>
</li>